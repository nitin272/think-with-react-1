const container = document.getElementById('react-container');

const element = (
  <div>
    <div>Hey Kalvians</div>
    <div>Let's rock and roll with classes</div>
  </div>
);

ReactDOM.render(element, container);
